
# RewardX - Complete Reward Application

RewardX is a cross-platform web app built with React, Supabase, and Gemini AI.

## 🚀 ফোল্ডার স্ট্রাকচার

- **Frontend/**: React (Vite) + Tailwind CSS + Supabase SDK.
- **Backend/**: Supabase SQL Migrations + Edge Functions + Gemini Logic.

## 🛠️ সেটআপ গাইড

### ১. এনভায়রনমেন্ট ভেরিয়েবল (.env)
আপনার প্রজেক্টের রুট ডিরেক্টরিতে একটি `.env` ফাইল তৈরি করুন:
```env
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
API_KEY=your_gemini_api_key
```

### ২. ডাটাবেস সেটআপ
- Supabase ড্যাশবোর্ডে যান।
- SQL Editor-এ গিয়ে `backend/migrations/001_schema.sql` ফাইলের কোড রান করুন।
- এটি প্রয়োজনীয় টেবিল এবং RLS পলিসি তৈরি করবে।

### ৩. লোকাল রান
```bash
# ডিপেনডেন্সি ইনস্টল করুন
npm install

# অ্যাপ রান করুন
npm run dev
```

## ✨ ফিচারসমূহ
- **Authentication**: ইমেইল এবং পাসওয়ার্ড দিয়ে লগইন।
- **Dynamic Quiz**: Gemini AI ব্যবহার করে প্রতিবার নতুন কুইজ জেনারেশন।
- **Real-time Wallet**: পয়েন্ট আপডেট হলে সাথে সাথেই ওয়ালেটে দেখা যাবে।
- **Cashout**: bKash এবং Nagad-এর মাধ্যমে টাকা তোলার ফর্ম।

## 🔐 সুরক্ষা (Fraud Detection)
- Supabase RLS ব্যবহারের ফলে ইউজার শুধু নিজের ডাটা দেখতে পারে।
- Edge Functions ব্যবহারের মাধ্যমে পয়েন্ট ক্যালকুলেশন সার্ভার-সাইডে করা যায়।
